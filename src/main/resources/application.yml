spring:
  datasource:
    url: jdbc:postgresql://roundhouse.proxy.rlwy.net:42543/railway
    username: postgres
    password: aIdufoKaiPUafnzUzxjpnwfqysyvrEpl
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  servlet:
    context-path: /api/v1/sakan/
project:
  house: house/

base:
  url: "http://sakan-production-7c44.up.railway.app/api/v1/sakan"
